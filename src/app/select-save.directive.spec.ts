import { SelectSaveDirective } from './select-save.directive';

describe('SelectSaveDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectSaveDirective();
    expect(directive).toBeTruthy();
  });
});
